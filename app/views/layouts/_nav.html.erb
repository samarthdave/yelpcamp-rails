<nav class="navbar navbar-default">
	<div class="container-fluid">
		<div class="navbar-header">
			<button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar-collapse-1" aria-expanded="false">
				<span class="sr-only">Toggle navigation</span>
				<span class="icon-bar"></span>
				<span class="icon-bar"></span>
				<span class="icon-bar"></span>
			</button>
			<!--<a href="/" class="pull-left"><img src="/img/logo.png" height="70"></a>-->
			<%= link_to 'YelpCamp', root_path, class: 'navbar-brand' %>
		</div>
		<div class="collapse navbar-collapse" id="navbar-collapse-1">
			<ul class="nav navbar-nav navbar-right">
				<% if !current_page?(controller: '/campgrounds', action: 'new') && user_signed_in? %>
					<li><%= link_to 'New Campground', new_campground_path %></li>
				<% end %>
				<% if !current_page?(controller: '/campgrounds', action: 'index') %>
					<li><%= link_to 'All campgrounds', campgrounds_path %></li>
				<% end %>
				<% if !user_signed_in? && !current_page?(controller: 'devise/sessions', action: 'new') %>
					<li><%= link_to 'Login', new_user_session_path %></li>
				<% end %>
				<% if user_signed_in? %>
					<li><%= link_to "Logout", destroy_user_session_path, method: :delete %></li>
				<% end %>
			</ul>
		</div>
	</div>
</nav>